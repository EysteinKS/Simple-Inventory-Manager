import React from 'react'
import { useSelector } from "react-redux"

import { useEditableList } from "../hooks/useEditableList"

//SELECT WHAT SUPPLIERS DELIVER THE PRODUCT

export default function ProductSuppliers({ suppliers = [], setSuppliers }){

  return (
    <div>
      
    </div>
  )
}
